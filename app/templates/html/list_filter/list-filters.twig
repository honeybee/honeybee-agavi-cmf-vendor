{%- block list_filters -%}
<div class="hb-list-filters {{ css }}">
    <div class="hb-list-filters__content">
        <ul class="hb-list-filters__list">
            {%- for filter_id, rendered_filter in rendered_list_filters %}
                {%- if filter_id in active_list_filters|keys %}
                    {{ rendered_filter|raw }}
                {%- endif %}
            {%- endfor %}
        </ul>
    </div>
    <div id="list_filter_templates" style="display:none;">
        {%- for filter_id, rendered_filter in rendered_list_filters -%}
            <script data-hb-filter-id="{{ filter_id }}" type="text/template">
                {{ rendered_filter|raw }}
            </script>
        {%- endfor -%}
    </div>
</div>
{%- endblock -%}