<a
    href="{{ link }}"
    rel="{{ activity.rels|join(' ') }}"
    class="{{ css|join(' ') }} {{ activity.settings.css|default('')|join(' ') }}"
    title="{{ _(activity.description, translation_domain) }}"
    data-hb-translation-domain="{{ translation_domain }}"
><img src="/static/themes/whitelabel/binaries/icons/favicon-16.png"> {{ _(activity.label, translation_domain) }}</a>