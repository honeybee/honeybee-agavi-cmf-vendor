{% block subheader_activities %}<!--
    -->{%- if rendered_subheader_activities is defined and rendered_subheader_activities is not empty -%}
    <div class="subheader-activities-box">
        <div class="subheader-activities">
            {{ rendered_subheader_activities|raw }}
        </div>
    </div>
    {%- endif -%}<!-- .subheader-activites-box
-->{% endblock subheader_activities %}

{%- block pagesubheader -%}
<div class="page-subheader-layout">
    <div class="subheader-context-box">
        <!-- <span class="page-title">{{ _page_title }}</span> -->

        <ul class="breadcrumb">
            {% for crumb in _breadcrumbs %}
                <li class="breadcrumb__item">{{ crumb|raw }}
            {% endfor %}
            <li class="breadcrumb__item page__title"><span>{{ _page_title }}</span>
        </ul>

    </div><!-- .subheader-title-box
-->{{ block('subheader_activities') }}
</div>
{%- endblock pagesubheader -%}