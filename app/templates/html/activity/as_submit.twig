<button type="submit" name="__submit" value="{{ activity.name }}" title="{{ _(activity.description, translation_domain) }}" class="{{ css|join(' ') }} {{ activity.settings.css|default('')|join(' ') }}" rel="{{ activity.rels|join(' ') }}" data-hb-translation-domain="{{ translation_domain }}" {% if disabled %}disabled="disabled"{% endif %}>{{ _(activity.label, translation_domain) }}</button>
